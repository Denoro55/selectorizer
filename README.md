Create simple wrapper for your selects and write your own styles for this.

## How to use:

```js
import selectorize from 'selectorizer';
import 'selectorizer/style.css';

const select = document.querySelector('.select');

selectorize(select);
```


----------------------------------------------------------

Alpha version!
